function tinhTong(so1, so2) {
  return so1 + so2;
}

alert('Tổng là: ' + tinhTong(5, 6));

let num1 = Number(prompt('Nhập số 1: '));
let num2 = Number(prompt('Nhập số 2: '));

alert('Tổng hai số vừa nhập là: ' + tinhTong(num1, num2));